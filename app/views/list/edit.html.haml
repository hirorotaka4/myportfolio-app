.listeditPage
  .container
    = form_with model: @list, url: { action: :update }, html: { class: 'listForm' }, local: true do |f|
      - if @list.errors.any?
        %p.text-danger タイトルは1～255文字以内で入力してください
      .field
        .field-label
          = f.label :title
        .field-input
          = f.text_field :title, autofocus: true, class: "listName", placeholder: "リスト名"
      .text-center
        = f.submit "編集", class: "submitBtn"