.listnewPage
  .container
    = form_with model: @list, url: { action: :create }, class: "new_list", local: true do |f|
      - if @list.errors.any?
        %p.text-danger titleは1～255文字以内で入力してください
      .field
        .field-label
          = f.label "リストを作成"
        .field-input
          = f.text_field :title, autofocus: true, class: "listName", placeholder: "リスト名"
      .text-center= f.submit "作成", class: "submitBtn"